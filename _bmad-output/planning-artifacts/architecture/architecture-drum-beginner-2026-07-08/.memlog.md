---
scope: Toàn hệ thống website học trống drum-beginner (React+TS, static, không backend)
purpose: Build substrate cho bước epics/stories + dev
altitude: initiative→features (toàn dự án greenfield)
updated: 2026-07-08T15:00
---

- (decision) Fast path + spine là deliverable duy nhất (user chọn); audience = chính user + AI dev agents ở bước story-dev
- (constraint) Kế thừa từ PRD/UX (binding): React+TS strict, static site không backend, localStorage + export/import, Web Audio lookahead (NFR-2 ±2ms), nội dung data-file khai báo (FR-2/SM-3), tiếng Việt, deploy static miễn phí (NFR-4), font Be Vietnam Pro self-host, dark-theme-only theo DESIGN.md tokens
- (decision) Paradigm: layered SPA 3 tầng ui→features→core+content; core (audio engine, progress store) và content thuần TS không import React [ASSUMPTION]
- (decision) Content-as-data: toàn bộ giáo trình (giai đoạn/tuần/bài/bài tập/video) là data file TS có type, component không hardcode nội dung; thêm Giai đoạn 2 = thêm data file (SM-3/FR-2)
- (decision) Metronome engine framework-free: class TS thuần, Web Audio lookahead scheduling (tick ~25ms + scheduleAhead ~100ms theo AudioContext.currentTime); React bind qua useSyncExternalStore; ngăn timing phụ thuộc render (NFR-2)
- (decision) Progress store duy nhất: một module sở hữu localStorage key versioned, schema có version cho migration; component chỉ mutate qua API store; export/import JSON = đúng envelope đó (FR-15/16/17)
- (decision) Styling: token DESIGN.md ánh xạ 1-1 thành CSS custom properties trong tokens.css; component chỉ dùng var(), không hex trần
- (decision) Audio là nguồn chân lý nhịp (kế thừa EXPERIENCE.md): engine phát beat event kèm audioTime; UI vẽ theo event, lệch ≤50ms; không CSS animation tự chạy song song âm thanh
- (question) Chọn host deploy (GH Pages vs Vercel vs Netlify) + router — chờ research verify SPA fallback hiện hành
- (version) Web-verified 2026-07-08: Vite 8.1 (Rolldown, create-vite react-ts vẫn canonical), React 19.2, TypeScript 6.0 (7.0 mới RC — không dùng), react-router 8.1 (package react-router, KHÔNG phải react-router-dom), Vitest 4.1, Zustand 5 (không cần), @fontsource/be-vietnam-pro 5.2, ESLint 10 flat-config-only + typescript-eslint unified
- (decision) Starter: npm create vite@latest -- --template react-ts (paved path, verify còn canonical); state dùng built-ins (Context+useReducer, useSyncExternalStore cho engine), KHÔNG Zustand ở scale này
- (decision) Deploy: Vercel static (SPA fallback native, zero config cho Vite, free tier) thay vì GH Pages (không có SPA fallback native, phải hack 404.html + base path) [ASSUMPTION — đổi sang Netlify/GH Pages đều được, chỉ ảnh hưởng config deploy]
- (decision) Styling: plain CSS + custom properties, không Tailwind/CSS-in-JS — ít component, token DESIGN.md map thẳng, dev agent không cần học config [ASSUMPTION]
- (decision) Assets tự host: âm thanh mẫu trống (public/sounds), font qua @fontsource — runtime không gọi CDN ngoài trừ YouTube embed click-to-load (NFR-5, PRD A4)
- (event) Reviewer gate + reconcile xong: lint 0; rubric PASS (1 med); versions PASS-có-điều-kiện (1 med-high, 2 med); adversarial 2 critical + 3 high (đường nối liên-feature); reconcile PRD 4 gap, UX 5 gap
- (decision) Vá A1/A2/A7/A8/A10/GAP-4: MetronomeBlock đặt ở ui/ (AD-1 cho phép ui→core); core/audio export MỘT instance engine singleton + MỘT AudioContext lazy-init theo user gesture; 'giải phóng audio'=stop()/suspend, cấm close(); tap() là API của engine (avg ≤5 tap, reset sau 2s); một hook useMetronomeShortcuts duy nhất trong MetronomeBlock; mount khối luyện tập: engine không chạy → set tempo bắt đầu của bài, đang chạy → giữ
- (decision) Vá UX-gap2 (tab ẩn): tick scheduling chạy trong Web Worker (main-thread timer bị throttle ≥1s khi background, đói lookahead 100ms); beat event payload {bar, beatInBar, audioTime} — engine là nguồn đếm duy nhất (A9)
- (decision) Vá A3-A6/A11-A13/GAP-2/GAP-3: MỘT không gian ID gd1-t2-b3 cho mọi item lộ trình (bài học lẫn bài tập — Exercise là phần nhúng của item, không có ID progress riêng); sessions chỉ ghi khi bấm 'Hoàn thành' (nhiều/ngày ok, completedLessons giữ timestamp đầu); streak/bài-tiếp-theo/N-trên-M là selector trong core/progress — UI không tự tính; import = validate shape đầy đủ rồi GHI ĐÈ toàn bộ sau xác nhận; load 3 trạng thái empty/ok/corrupt; content/ sở hữu lookup getLessonById
- (decision) Vá GAP-1 + F-2 rubric: video type thêm note (bắt buộc khi lang=en, tóm tắt tiếng Việt); thứ tự hiển thị = thứ tự data, component không sort; ngoại lệ CDN mở rộng: iframe YouTube sau click + ảnh thumbnail i.ytimg.com (ảnh tĩnh)
- (decision) Vá stack theo lens versions: Vercel cần vercel.json rewrites (/(.*)→/index.html) — không native; lint theo starter default = oxlint (bỏ ESLint 10 khỏi seed); Node ≥22.12 LTS (20 EOL 04/2026); RTL bỏ khỏi seed (defer đến khi có UI test); quality gate = build command Vercel chạy npm run check (tsc --noEmit + oxlint + vitest run + vite build)
- (decision) Vá UX-gap3: convention accessibility — styles/global.css sở hữu :focus-visible + prefers-reduced-motion (một chỗ); reduced-motion: beat đổi màu không scale; breakpoint 768px + responsive override (pattern-letter 56→40px) = media query đổi custom property trong tokens.css (AD-5)
- (assumption) Triage non-blocker (giữ, revisit khi chạm): Vercel host (đổi Netlify/GHPages chỉ ảnh hưởng config), plain CSS không Tailwind, không UI framework — cả ba tagged [ASSUMPTION] trong spine
- (event) spine finalized
